<% provide(:title,@user.name)%>
<div class="row">
 <aside class="col-md-4">
  <section class="user_info">
   <h1>
    <%= gravatar_for @user %>
    <%= @user.name %>
   </h1>
  </section>
 </aside>
</div>
<%= Time.now %><P>

<%= @user.email %><p>
<%= @user.created_at %><p>
<%= @user.updated_at %><p>
